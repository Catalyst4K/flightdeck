# Copy to .env (gitignored) for local dev/packaging. Never commit real values here.

# Nothing needed here for SimBrief generation — the signing key lives server-side in
# flightdeck-backend (docs/decisions.md, 2026-09-04), not baked into this build.
