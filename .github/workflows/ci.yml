name: CI

# Placeholder — this project isn't scaffolded yet (no package.json). Claude Code
# should fill this in as part of M0: install deps, then run lint, typecheck and test
# on every push and pull request against main. Delete this comment once real steps
# are in place.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: TODO
        run: echo "Replace with actions/setup-node, npm ci, npm run lint, npm run typecheck, npm test"
